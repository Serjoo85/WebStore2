@model IEnumerable<WebStore.Models.Employee>
<div class="container">
    <table class="table">
        <thead>
            <tr>
                <th>Фамилия</th>
                <th>Имя</th>
                <th>Отчество</th>
                <th>Возраст</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var employee in Model)
            {
                <tr>
                    <td>
                        <a asp-controller="Employees" asp-action="Detailes" asp-route-id=@employee.Id>
                            @employee.LastName
                        </a>
                    </td>
                    <td>@employee.FirstName</td>
                    <td>@employee.Patronymic</td>
                    <td>@employee.Age</td>
                </tr>
            }
        </tbody>
    </table>
    <hr/>
    <p>
        <a asp-controller="Home" asp-action="Index">Home</a>
    </p>
</div>

